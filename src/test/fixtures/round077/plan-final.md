<!-- Generated by Claude Code (fable@high) -->

<!-- ensemble:implementation-checklist -->

# Implementation Checklist: Workflow-robustness fixes — stranded rounds, misclassified failures, and unbreakable loops

## Goal

Fix eight workflow-robustness defects as one task in eight ordered parts: stranded deferred rounds get one shared durable recovery transition (Part 1) with evidence-based, enforced recovery modes (Part 2); the impl↔review loop gains real exits (Part 3); entitlement 403s, quota reset times, benign CLI noise, silent hangs, and cross-reviewer score comparison are each fixed in Parts 4–8.

## Part 1 — One durable recovery transition for every unreported round (item 1)

- [x] Create a test fixture from the round-010 response body (`.ensemble/2026-08-13_task_1/runs/010-claude-cli-impl.md`; reconstruct from the context-pack excerpts if archived) and run it through `describeIncompleteImplementationRoundV1` and `describeImplementationSummaryShapeIssue` to pin exactly which gate let it finalize.
- [x] Tighten `describeIncompleteImplementationRoundV1` in `src/utils/implementationArtifactResolver.ts` (339–379): a response matching `DEFERRAL_PHRASES_V1` that is missing any required section — or whose sections are empty when the round changed files — classifies as `roundDeferred` even when one section is present; complete well-shaped responses with an incidental phrase match stay accepted.
- [x] Extract the existing deferred/incomplete-branch persistence and dispatch logic (`src/commands/reviewActions.ts` 5374–5521) into a single shared seam `beginImplementationRecoveryV1(folderUri, input)`, preserving the existing quarantine-before-run-log durable ordering exactly (deferredRoundRecovery.test.ts must stay green unmodified except for new assertions).
- [x] Route the stamped-unusable-summary path (reviewActions.ts 5715–5736, `return false` at 6002) through `beginImplementationRecoveryV1` so it persists recovery state and dispatches instead of persisting nothing.
- [x] In `beginImplementationRecoveryV1`, persist in a single `patchTaskProgressStrictV1` call before any run-log, artifact, or scheduling write: (a) the round's change set quarantined into `pendingImplReviewFiles` from the run record's git-snapshot `filesChanged`, recording `filesChangedUnknown` explicitly rather than an empty list; (b) the `incompleteRoundContinuations` increment bounded by `MAX_INCOMPLETE_ROUND_CONTINUATIONS_V1` with the same cap-reached escalation; (c) a new `implRecovery` record carrying `sourceAttemptId`, `reason`, triggering failure class, recovery mode, and dispatch state.
- [x] Add `implRecovery` to both type declarations (`src/types/taskProgress.ts`, `packages/ensemble-core/src/taskProgressV1.ts`), both strict decoders' allowed-key sets (`src/services/taskProgressDecoderV1.ts` and the ensemble-core mirror), and the field policy (`src/services/taskProgressFieldPolicyV1.ts`).
- [x] Implement the `implRecovery.dispatch` state machine (`pending` → `dispatched` → cleared) carrying `leaseOwner` / `leaseUntil` / continuation `attemptId`: transition commits `pending`; dispatcher claims lease and flips to `dispatched` with the attemptId in one patch; on task load/activation a `pending` record with no live lease (cap not reached) re-arms the chain; a `dispatched` record whose attemptId already produced a round never re-dispatches; the record clears in the same transaction that finalizes a subsequent usable summary. Arm re-dispatch through `TaskActionScheduler` (`src/commands/scheduleTaskResume.ts`) where practical to inherit lease renewal and the stage-moved skip guard.
- [x] Update the unusable-path warning and run log to state that a continuation was scheduled ("continuation N of 3", naming the recovery mode) or that the cap was reached and a human is needed — wording distinct from "review paused, waiting on user" (reuse the incomplete-branch notification style at reviewActions.ts 5464–5502).
- [x] Add regression tests in `src/test/implementationSummaryArtifact.test.ts` and `src/test/deferredRoundRecovery.test.ts`: waiter-narration-only → `roundDeferred`; waiter narration plus exactly one section present → `roundDeferred`; the round-010 fixture lands the identical recovery record via either gate before anything is scheduled; a stamped-unusable summary without deferral phrasing also lands it; simulated restart after persistence but before dispatch re-arms exactly once and a `dispatched` record never double-fires; cap exhaustion escalates instead of looping; `filesChangedUnknown` recorded honestly.

## Part 2 — Enforceable recovery modes instead of a full stage redo (item 2)

- [x] Reconstruct from `.ensemble/2026-08-13_task_1`'s task-progress history and run logs what regressed `currentStage` from `impl-high-review` to `impl` and dropped `impl` from `completedStages`; if an automatic path, gate it; if manual/legacy, record the finding and proceed.
- [x] Implement recovery-mode selection in `beginImplementationRecoveryV1`, persisted at transition time: `summary-only` iff the process terminated normally AND the latest `impl-high-review` passed with 0 blockers over the pre-round boundary AND the delta is known; `inspect-and-complete` when the round was terminated externally (timeout, inactivity kill, crash) with a known non-empty change set; `unconstrained` when edits are suspect (open blockers, no passing review, or `filesChangedUnknown`).
- [x] Write the continuation mandates: `summary-only` produces `## Files Changed` / `## Verification` / checklist echo covering the existing combined diff (reviewed files plus quarantined delta) with no edits; `inspect-and-complete` verifies quarantined files for partial edits, finishes or reverts what is incomplete, then reports, restricted to the quarantined-plus-reviewed boundary (touch `resources/prompts/run-implementation.md` / `apply-impl-review-code.md` as needed).
- [x] Dispatch `summary-only` continuations through the broker in `mode: "text"` (edit permissions withheld, per the existing `src/commands/chatWithStage.ts:102` usage); when the stage's resolved provider cannot honor text mode, fall back to `inspect-and-complete` rather than issuing an edit run with a no-edits instruction.
- [x] Add the post-run delta gate on every recovery continuation: a `summary-only` continuation with any non-empty delta is rejected as a summary-only report, its delta quarantined into `pendingImplReviewFiles` (never discarded or banked as reviewed), the mode escalated to `inspect-and-complete` for the next continuation under the same cap, with warning and run log stating it edited files it was not permitted to edit; for `inspect-and-complete`, files touched outside the quarantined + reviewed boundary are recorded as unreviewed scope and named in the run log.
- [x] Verify and pin with a test that the stale-review mechanism forces a fresh review of the combined scope after any recovery continuation — the prior score never covers the quarantined delta.
- [x] Add Part 2 tests: normal-termination reporting-only failure after a 0-blocker review → `summary-only` in text mode scoped to reviewed files + quarantined delta; same failure with open blockers or unknown delta → `unconstrained`; externally-killed round with known delta → `inspect-and-complete`, never `summary-only`; a `summary-only` continuation that edits files → rejected, delta quarantined, mode escalated, cap respected.

## Part 3 — Checklist ticks that can never land, and the loop that never exits (item 8)

- [ ] Create a fixture from the actual round-013 output (`.ensemble/2026-08-11_task_2/runs/013-*.md`; reconstruct from excerpts if archived), run it through `mergeChecklistProgressV1` (`src/utils/implementationChecklist.ts` 586–718), and determine why `collectRetroactiveTickClaimsV1` produced zero ticks (line shape outside recognized rendering, echo-scoping via `splitSummaryAtEchoV1`, normalization mismatch, or missing ` — ` separators).
- [ ] Fix the confirmed merge/scoping defect in `src/utils/implementationChecklist.ts` — no general "a `— done` bullet counts as a tick" heuristic — and mirror the change in `packages/ensemble-engine/src/checklistProgressV1.ts` with its parity tests.
- [x] Treat a summary that both declares `<!-- ensemble:no-checklist-change -->` and supplies retroactive/done claims as self-contradictory: a shape issue via `assessImplementationSummarySectionsV1` / `describeImplementationSummaryShapeIssue`, rejecting the round with an actionable message naming the exact syntax to use.
- [x] Strengthen `resources/prompts/run-implementation.md` and `resources/prompts/apply-impl-review-code.md` with a worked example of retroactive tick syntax and an instruction never to combine it with the no-change marker.
- [x] Extend the `checklistProgressUnreliable` trigger (reviewActions.ts 5829–5846) to also fire when a round claimed completions but no tick merged (retroactive claims present, or merge returned `no-match` with unmatched samples, yet zero ticks landed), standing the completeness gate down and surfacing `reconcilePlanChecklist` as the remedy.
- [x] Establish from the "1.9" run records why the existing no-progress breaker did not trip across rounds 009/011/013 (none-recorded file state, field-policy clear on stage transition, or window reload predating the counter) and document the confirmed cause in the fix.
- [ ] Generalize the single existing breaker: extend the `zeroChangeImplRounds` increment condition (renamed/extended in place through decoders and policy if semantics widen) to cover a completed implementation round with no file delta AND no checklist delta — including claimed-but-unmerged and the none-recorded state once pinned — following a review at/above threshold; keep the one counter, one setting (`ensemble.resilience.noProgressBreakerRounds`), one escalation path, extending the escalation message to name the checklist gap and `reconcilePlanChecklist` when diagnosed.
- [x] Add Part 3 tests: fixture-driven merge test reproducing round 013; contradictory-marker rejection; latch fires on claimed-but-unmerged; generalized breaker trips on sterile rounds including the none-recorded state; existing `shouldTripNoProgressBreaker` / resilienceDefaults tests and `packages/ensemble-engine/tests/checklistParity.test.ts` stay green.

## Part 4 — Model-entitlement 403s misclassified as auth (item 3)

- [x] Add `isModelEntitlementFailure(message)` to `src/utils/quota.ts` matching "is not available for this account" plus an extensible per-provider marker list — the discriminator is the phrasing, never the 403 status alone.
- [x] Extend the `failureKind` union (quota.ts 148–155) with `"model-entitlement"`; in `classifyFailure`, check entitlement before the auth gate so `isAuthenticationFailure`'s `forbidden`/`403` patterns never claim these messages; sweep every `failureKind` consumer (runnerRegistry cascades, quota observation, retry logic, tests) for exhaustiveness.
- [x] In `toFriendlyError` (`src/runners/cliAgentRunner.ts` 2219–2242), an entitlement match forces `authFailure` false, skips the `loginHint` suffix, and appends switch-the-stage's-model advice (keeping the provider's own "explore other available models" text).
- [x] Add `"model-entitlement"` to the cascade-eligible kinds in `src/runners/runnerRegistry.ts` (implementation-mode gate 857–864 and withheld-branch condition 975–981) with an appropriate `limitLabel`, so `switch-to-backup` fires on a clean tree and the dirty-tree gate (854–856) withholds with its explanation; mirror the text-mode cascade gate (line 570) if applicable.
- [x] Map `"model-entitlement"` in `recordQuotaObservation` (quota.ts 235–248) to a new `"entitlement-blocked"` `QuotaState` and render it in `formatQuotaStatus` so a blocked model never reads "OK as of …".
- [x] Add Part 4 tests (`src/test/quota.test.ts`, `src/test/cliFailureClassification.test.ts`): Bedrock-style 403 classifies `model-entitlement`, non-auth, no login hint; cascade fires on a clean tree with `switch-to-backup` configured; withheld-with-explanation on a dirty tree; existing 401 / credential-403 / balance-401 cases keep current behavior.

## Part 5 — Parse quota reset times; branch the remedy on magnitude; persist it (item 4)

- [x] Add `parseQuotaResetV1(message, now)` to quota.ts handling clock-time ("resets 12:10am (Asia/Jerusalem)" via `Intl`, rolling to tomorrow when past) and relative-duration ("resets in 8d 19h", "in 45 minutes", "in 2h 30m") shapes; return ISO `resetAt` or `undefined` on any ambiguity.
- [x] Add a parked-on-quota record to TaskProgress (`{ modelId, providerId, accountKey?, failureKind, resetAt?, observedAt }`) written in the same transaction that parks the stage, through both type declarations, both decoders, and the field policy.
- [x] Add a workspace-independent provider-outage ledger in `ExtensionContext.globalState` keyed by provider + account/credential context + model id; make the in-memory `quotaObservations` Map (quota.ts 223) a cache/view over it; give `recordQuotaObservation` an optional `resetAt` written through; implement clearing rules (a later success or contradicting fresh observation clears; `resetAt` merely passing does not auto-clear).
- [x] Branch the parked/withheld remedy messages (runnerRegistry.ts 983–999 and analogous notifications) from the persistent record: reset within the threshold → "rerun after <time>" plus opt-in timed resume; beyond it → provider out until the concrete date, switch-model advice, no rerun offered; no known reset → today's wording. The threshold is a named constant (proposed 24h) with a setting override.
- [x] Add a programmatic `TaskActionScheduler` entry point that persists `scheduledRun` at `resetAt` plus a small buffer without the interactive input box, wired to a "Rerun after reset" notification action; verify the scheduled fire performs the normal pre-run checks (dirty-tree awareness, fresh quota observation) and add whatever is missing.
- [ ] Add the long-outage stage-impact notice: when the reset is beyond the threshold, enumerate stages whose effective primary chain (`resolveEffectiveStageChainV1`) uses the blocked provider and include them in the notification.
- [x] Add Part 5 tests: both formats parse (timezone rollover, the 8d 19h case); threshold branching produces the right remedy text; unparsable degrades to current behavior; parked record and globalState ledger survive a simulated host restart and still drive the correct remedy; two accounts sharing a model id do not cross-contaminate; the scheduler arms at the parsed time via the injectable `SchedulerClock`; clearing rules honored.

## Part 6 — Known-benign CLI noise never presented as the diagnosis (item 5)

- [x] Add `KNOWN_BENIGN_CLI_ERROR_SIGNATURES_V1` in cliAgentRunner.ts, seeded with the Cline `hook dispatch failed: session.hook requires a valid hook event payload` line, with a per-signature remediation hint (known Cline shutdown-race artifact; often cleared by `cline doctor fix` / restarting the local hub daemon).
- [x] Filter at `toFriendlyError` composition (2229–2244): when the only diagnosable content — structured `detail`, the `unparsedText` fallback (1606–1617), or the stderr fallback — consists solely of benign-signature matches, present "exit code N / no diagnostic output" plus the hint; when real error content coexists, surface the real content; route both confirmed raw-JSON leak paths (re-serialization at 1576–1580 / 1596–1601 and the `parseJsonLineEvents` unparsed-line fallback at 705–753) plus the stderr branch through the filter.
- [x] Confirm the ~6-minute post-error hang from the incident is observable in event-stream timestamps (the watchdog itself is Part 7).
- [x] Add Part 6 tests (extending cliFailureClassification.test.ts 664–724): failed run whose only error content is the hook-dispatch line (structured, and again via unparsed/stderr) → generic unknown-cause message with the doctor hint; failed run with a real error plus the noise → the real error surfaces.

## Part 7 — Inactivity watchdog and an honest timeout message (item 6)

- [x] Track `lastActivityAt` on every stdout/stderr chunk in `execCliAgent` (handlers at cliAgentRunner.ts 2559–2569) and on raw chunk arrival in `createCliTextTransportV1` (1968–1990, keyed off bytes, not `onProgress`); add a periodic check that kills the process tree when silence exceeds the limit and finishes through the existing timeout classification path with a distinct inactivity classification ("produced no output for N minutes"); prefer subprocess-liveness evidence over stdout-only silence where observable.
- [x] Add the inactivity-limit setting under `ensemble.*` (conservative default, proposed 15 minutes; 0 disables), auditable in the run record.
- [x] Restructure the timeout message so the final user-facing text is composed after the git snapshot computes `filesChanged` (3265–3287): known-dirty → "timed out after changing N file(s): [list] — review these before retrying"; known-clean → "left the working tree clean"; keep the "may already have made changes" hedge only when `filesChangedUnknown` is true; surface the list in the notification and run record, not only the run file.
- [x] Route timed-out or inactivity-killed edit rounds with a known non-empty change set into `beginImplementationRecoveryV1` with mode `inspect-and-complete` (same single-transaction quarantine, counter, leased dispatch); `filesChangedUnknown` routes to `unconstrained`.
- [x] Add or verify a log line distinguishing "no backups configured" from "tree state unknown" (the `filesChangedUnknown === true` fall-through at runnerRegistry.ts 971–974) so the observed branch is diagnosable from the run record.
- [x] Add Part 7 tests (extending `src/test/cliRetryEvidence.test.ts`): watchdog fires on silence and not on a slow-but-active stream (fake timers); inactivity classification distinct from wall-clock timeout; the three message variants; a timed-out round with known changes enters the Part 1 transition with mode `inspect-and-complete`, and with unknown tree state gets `unconstrained`.

## Part 8 — Review scores compared across a changing reviewer (item 7)

- [ ] Add optional structured reviewer identity (provider id, resolved model id, reasoning tier/effort) to `ReviewScoreHistoryEntry` in both type declarations (`src/types/taskProgress.ts` 394–410, `packages/ensemble-core/src/taskProgressV1.ts` 229–244), both strict decoders' allowed-key sets, and a field-policy row (taskProgressFieldPolicyV1.ts 206–211 plus pass-through lists).
- [ ] Thread `outcome.provider` (`TaskActionOutcomeProviderV1`, carrying the actually-invoked model including backup-cascade substitutions) into `handleReviewRoutingOutcome`'s options (reviewActions.ts 1556–1563, call site 1992–1998) and populate the entry at 1634–1642.
- [ ] In `improveReviewScore` (`src/utils/reviewScoreLoop.ts` 302–307), when the current round's reviewer identity differs from the baseline's, do not apply the +0.1 delta test across the break — prefer blocker-count movement for that first round or re-baseline to the new reviewer's first score; entries without a recorded identity keep today's behavior.
- [ ] Restrict `detectPlateau` (reviewRouting.ts 54–82) and `detectBlockerSetStall` (140–185) comparison windows to the trailing run of entries from the current reviewer; a window spanning an identity change returns not-plateaued.
- [ ] Add Part 8 tests: decoder/field-policy acceptance of the new field; reviewRouting.test.ts cases for an identity change inside the window; reviewScoreLoop.test.ts case for baseline-vs-new-reviewer; legacy entries without the field behave exactly as before.

## Verification

- Run `check-types`, `lint`, and `verify:workflow-safety` — all must pass after every part.
- Run the full test suite green, including the new regression tests listed per part and the ensemble-engine parity tests (`packages/ensemble-engine/tests/checklistParity.test.ts`).
- Part 1: replay the round-010 fixture end-to-end — whichever gate catches it, the identical recovery record (quarantined delta, source attempt, continuation count, mode, `dispatch: pending`) persists before anything is scheduled; simulate a host restart between persistence and dispatch and confirm exactly one re-arm and no double-fire; confirm no path leaves a task at `active` with an unusable summary, nothing scheduled, and no distinct signal.
- Part 2: confirm mode selection matches the evidence table (normal termination + 0-blocker review → `summary-only` in text mode; external kill → `inspect-and-complete`; suspect/unknown → `unconstrained`); confirm an edit-making `summary-only` continuation is rejected, quarantined, escalated; confirm the combined scope always requires a fresh review; record the confirmed cause of the observed stage regression.
- Part 3: replay the round-013 fixture — ticks merge (or the round is rejected with actionable feedback); confirm the latch fires on claimed-but-unmerged completions and the generalized breaker terminates a sterile impl↔review loop; document the confirmed reason the breaker previously failed to trip.
- Part 4: replay the Bedrock-style 403 — classifies `model-entitlement`, no login hint, switch-model advice, cascade fires on a clean tree / withheld-with-explanation on a dirty tree; settings quota view shows `entitlement-blocked`, never OK.
- Part 5: replay both quota messages — near reset offers a timed resume, far reset (8d 19h) advises switch-model only with no rerun implied; records survive a host restart; the scheduled rerun performs the same pre-run checks as a manual one.
- Part 6: replay a failure whose only error content is the hook-dispatch line via structured and unparsed/stderr paths — the generic unknown-cause message with doctor hint appears, never the raw JSON.
- Part 7: verify a silent hang is killed at the inactivity limit with the distinct classification, timeout messages name the concrete change set or clean tree, and known-change timeouts land in `inspect-and-complete` recovery.
- Part 8: verify new history entries carry reviewer identity and neither score-delta nor plateau logic compares across a reviewer substitution, with legacy entries unaffected.
- Confirm no unrelated files were refactored.
