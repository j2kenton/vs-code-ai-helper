# Ensemble design tokens

The task tree uses VS Code theme variables so it remains accessible in light,
dark, and high-contrast themes. Active stages use `--vscode-focusBorder`,
paused stages use `--vscode-descriptionForeground`, and errors use
`--vscode-errorForeground`.
