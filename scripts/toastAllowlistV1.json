{
  "$schema": "n/a — read by scripts/verifyToastAllowlistV1.mjs",
  "mode": "enforcing",
  "note": "Every vscode.window.showInformationMessage/showWarningMessage call under src/ that offers MORE THAN ONE button must either be converted to a WorkflowDecisionV1/StructuredQuestionV1 posted in the chat, or be listed here with a reason it genuinely is NOT a workflow decision (task 'stage chat as a record of work', item 13 / Part 12 step 35e). A single-button call never needs an entry — the lint only flags calls that ask the user to choose between options. Entries are keyed by file:line, so a line-number drift on an untouched call is expected to surface as a stale entry on the next run — update the line, do not delete the entry, unless the call itself was removed or reduced to one button.",
  "entries": [
    {
      "file": "src/commands/commitAndPushTask.ts",
      "line": 336,
      "reason": "Direct synchronous confirmation inside the user-invoked Commit & Push flow — the modal preview of the exact commit message and destination before it ships, with an inline 'Regenerate' retry. Not a review/round workflow decision. (Line moved 332 -> 333 -> 334 -> 335 -> 336 as the 2026-09-11 target-resolution write-failure fix, the shared early-admission-helper wiring fix, the 2026-09-14 unprotectedRootPaths check, and the same day's pause-resolver import each added an import/block above it; call is unchanged.)"
    },
    {
      "file": "src/commands/commitAndPushTask.ts",
      "line": 1797,
      "reason": "Direct synchronous confirmation inside the user-invoked Publish flow when completion checks fail — 'Publish Anyway' vs 'Fix with AI' is answered in the same synchronous command invocation, not posted from a background round. (Line moved 1793 -> 1794 -> 1795 -> 1796 -> 1797, see commitAndPushTask.ts:336's note.)"
    },
    {
      "file": "src/commands/commitAndPushTask.ts",
      "line": 2191,
      "reason": "Direct synchronous confirmation inside the user-invoked Commit & Push flow, offering to view the full file list before an outward-facing, largely irreversible push. (Line moved 2187 -> 2188 -> 2189 -> 2190 -> 2191, see commitAndPushTask.ts:336's note.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 8463,
      "reason": "Direct synchronous response to the user explicitly invoking 'View Review' when no review artifact exists yet — offers to dispatch a review or create the file by hand. Not a decision surfaced from an automated round. (Line moved 8010 -> ... -> 8388 -> 8399 -> 8436 -> 8462 as the fixes noted in prior revisions of this entry, then a later 2026-09-18 v1 fixes 2 review-fix round's item-1 fix (Fast Forward's unusable-summary restore action, built in two edits) added 63 lines above it in total; call is unchanged.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 9759,
      "reason": "Pre-run safety check synchronously inline in the user-invoked 'Run Implementation' command (unrelated uncommitted changes in the workspace) — a git-safety confirmation, not a review/round outcome. (Line moved 9181 -> ... -> 9665 -> 9695 -> 9732 -> 9758 as the fixes noted in prior revisions of this entry, then a later 2026-09-18 v1 fixes 2 review-fix round's item-1 fix (Fast Forward's unusable-summary restore action, built in two edits) added 63 lines above it in total.)"
    },
    {
      "file": "src/commands/runLintingFixes.ts",
      "line": 578,
      "reason": "Pre-run safety check synchronously inline in the user-invoked 'AI final fixes' command, mirroring reviewActions.ts:7690 for the same git-safety concern in a different entry point."
    },
    {
      "file": "src/extension.ts",
      "line": 2045,
      "reason": "Activation-time recovery for an interrupted revert-journal swap found on disk — the plan's own named example of an expected allow-list entry ('revert-journal restore'). Resolves a durable mid-flight artifact, not a task review/round decision."
    },
    {
      "file": "src/utils/aiConsent.ts",
      "line": 57,
      "reason": "The one-time legal/usage disclaimer gate shown before the very first AI command of a session — modal by design, and 'View Disclaimer' loops back rather than branching workflow state. Not a task decision."
    },
    {
      "file": "src/utils/metaResourcesMigration.ts",
      "line": 365,
      "reason": "One-time workspace housekeeping offer (move a legacy resource folder to .ensemble) shown at most once per workspace, unrelated to any task's review/round workflow."
    },
    {
      "file": "src/utils/promptSizeGuard.ts",
      "line": 92,
      "reason": "Direct synchronous pre-dispatch size warning inline in whichever command is about to send an oversized prompt — a cost/quota guard on the calling command's own flow, not a review/round outcome to record in chat."
    },
    {
      "file": "src/runners/copilotImplementationRunner.ts",
      "line": 806,
      "reason": "Fallback of the already-migrated round-limit gate (see the awaitWorkflowDecisionAnswerV1 call a few lines above) for the one case with no task to attribute a chat decision to (no taskFolderUri/stage in scope). The task-scoped path never reaches this line. (Line moved 794 -> 806 as the 2026-09-15 post-freeze findings item 5 workflow-control exclusion added to executeToolCall's write_file/delete_file tool handlers, above it, added 12 lines.)"
    },
    {
      "file": "src/config/settings.ts",
      "line": 328,
      "reason": "Activation-time notice that workspace and user settings values differ, offering to reconcile them — a settings-hygiene notice unrelated to any task's review/round workflow, and not gating task progress."
    },
    {
      "file": "src/views/settingsView.ts",
      "line": 566,
      "reason": "Settings-view notice about leftover per-task model overrides from a superseded configuration shape, offering to clear them — settings hygiene, not a task workflow decision."
    }
  ],
  "directActionEntriesNote": "Every NotificationRouter.show(Information/Warning/Error) call under src/ whose actionCommand dispatches a workflow command directly (rather than the chat-pointer vs-code-ai-helper.openWorkflowDecision) must either be converted to point at a WorkflowDecisionV1 in the chat, or be listed here with a reason it is NOT a hidden workflow decision (task 'stage chat as a record of work', item 13 / Part 12 step 35e, review round 2). Two shapes qualify: (1) a synchronous gate/nudge inline in the user-invoked command it belongs to, pointing at a command already reachable another way (Command Palette, another button) and never the only record of a background round's outcome; (2) a best-effort fallback for when a WorkflowDecisionV1 could not be posted to chat (no activating extension context), degrading to a single pointer at the same option the decision would have recommended.",
  "directActionEntries": [
    {
      "file": "src/commands/chatWithStage.ts",
      "line": 742,
      "reason": "Synchronous pre-dispatch gate inline in the user-invoked Chat Send command when no model is configured for the stage — points to Ensemble Settings to fix the configuration, not a review/round decision. (Line moved 589 → 601 → 612 → 605 → 633 → 653 → 659 → 667 → 677 → 689 → 695 as admission-registration edits, including the 2026-09-14 ownership-legitimacy-signal doc-comment update, landed on this route; call is unchanged.)"
    },
    {
      "file": "src/commands/draftTaskWithAI.ts",
      "line": 420,
      "reason": "Synchronous pre-dispatch gate inline in the user-invoked Draft with AI command when no model is configured for the Description stage — points to Ensemble Settings, not a review/round decision. (Line moved 384 → 385 → 386 → 385 → 417 → 418 → 419 as admission-registration edits, including the 2026-09-14 unprotectedRootPaths check, landed on this route; call is unchanged.)"
    },
    {
      "file": "src/commands/generatePlanWithAI.ts",
      "line": 734,
      "reason": "Synchronous pre-dispatch gate inline in the user-invoked Generate Plan command when no model is configured for the Plan stage — points to Ensemble Settings, not a review/round decision."
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 12316,
      "reason": "Synchronous pre-dispatch gate inline in the create-implementation-checklist path when no model is configured for Implementation — points to Ensemble Settings, not a review/round decision. (Line moved 11736 -> ... -> 12242 -> 12252 -> 12289 -> 12315, see reviewActions.ts:9695's note; a later 2026-09-18 review-fix round's item-1 fix (built in two edits) shifted it by 63 more in total.)"
    },
    {
      "file": "src/commands/markTaskDone.ts",
      "line": 183,
      "reason": "Direct synchronous response to the user-invoked Complete Task command when the Publish artifact is missing — 'Complete Anyway' re-invokes the same command with an explicit override, not a decision surfaced from a background round."
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 5143,
      "reason": "(Line moved 5091 -> 5140 -> 5141 -> 5142: the fixes noted in prior revisions of this entry, then the 2026-09-18 v1 fixes 2 review-fix round's toast-allowlist refresh shifted it by 1 more.) Direct synchronous refusal inline in runReviewForFolder's own unusable-implementation-summary gate — offers to restore impl-summary_prev.md when it is itself usable (2026-09-15 post-freeze findings, item 3: a refusal must name a remedy the system can actually reach). restoreRejectedImplementationRoundV1 is a deterministic artifact restore already reachable from the pre-existing 'Discard Last Round' decision; this only offers the same action at a second refusal surface. Not a review/round outcome posted from a background round. (Line moved 4973 -> 4988 -> 4989 -> 4994 -> 5023 -> 5032 -> 5081 -> 5091 when the completion-review fix threaded a rerunCommandId option through runReviewForFolder so this button can also re-dispatch the refused stage after a successful restore, then the same day's withDeterministicZeroFilesChangedNoteV1 import addition shifted it by one more line, then the same day's post-freeze findings item 5 import additions noted at reviewActions.ts:8329 shifted it by 5 more, then the same day's post-freeze findings item 4 quotaCandidate rework noted at reviewActions.ts:8329 shifted it by 29 more, then that same finding's typed deferredFailureKind follow-up noted at reviewActions.ts:8329 shifted it by 9 more, then that same finding's run-log quota-block note shifted it by 49 more, and then the 2026-09-16 candidatesDeferred outcome-code addition noted at reviewActions.ts:8329 shifted it by 10 more.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 7366,
      "reason": "v1 fixes 2, item 1 (Wave II escape hatches): Fast Forward's OWN unusable-implementation-summary refusal — the third of the three surfaces that must offer 'Restore Last Usable Summary' (the other two are reviewActions.ts:5142's runReviewForFolder branch, and buildReviewResumeVariablesV1's identical branch, which returns its actionCommand to a caller rather than calling NotificationRouter itself). Same restoreRejectedImplementationRoundV1 deterministic artifact restore, reached when Fast Forward tries to run its own initial review before fast-forwarding and that dispatch is refused for the same rejected-round reason. Not a review/round outcome posted from a background round — offered from the same synchronous command invocation the user's Fast Forward click started."
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 7389,
      "reason": "Same as reviewActions.ts:7365 immediately above — Fast Forward's second unusable-summary check point a few lines later (the re-read after runReviewForFolder's own initial-review dispatch attempt comes back with no parseable Readiness score), offering the identical restore action for the identical cause."
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 8802,
      "reason": "(Line moved 8617 -> 8708 -> 8728 -> 8738 -> 8775 -> 8801, see reviewActions.ts:9695's note.) Direct synchronous 'Complete Anyway' gate inline in the stage-advance command when a required completion artifact is missing — re-invokes the same command with an explicit override, the same pattern as markTaskDone.ts:183. (Line moved 8251 -> ... -> 8617, see reviewActions.ts:8329's note; a later 2026-09-18 review-fix round's item-1 fix (built in two edits) shifted it by 63 more in total.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 8836,
      "reason": "(Line moved 8651 -> 8742 -> 8762 -> 8772 -> 8809 -> 8835, see reviewActions.ts:9695's note.) Direct synchronous 'Complete Anyway' gate inline in the stage-advance command when a plan-review blocker is not recorded as resolved — re-invokes the same command with an explicit override, the same pattern as markTaskDone.ts:183. (Line moved 8285 -> ... -> 8651, see reviewActions.ts:8329's note; a later 2026-09-18 review-fix round's item-1 fix (built in two edits) shifted it by 63 more in total.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 8919,
      "reason": "(Line moved 8734 -> 8825 -> 8845 -> 8855 -> 8892 -> 8918, see reviewActions.ts:9695's note.) Direct synchronous refusal inline in advanceStageViaNextStageRowV1's own catch block (the 'Complete Stage & Move On' command) when the transition was refused for an owed implementation recovery continuation that has no automated way back (2026-09-15 post-freeze findings, item 5 / Part 5 step 34: 'a dispatched recovery needs a user-reachable escape'). discardOwedImplRecoveryV1 is a confirmed, deterministic clear of the stuck record, offered only when isImplRecoveryDiscardOfferableV1 confirms the record cannot resolve itself. Not a review/round outcome posted from a background round — this is the same synchronous command invocation the user just clicked. (Line moved 8637 -> ... -> 8734, see reviewActions.ts:8329's note; a later 2026-09-18 review-fix round's item-1 fix (earlier in this same file, built in two edits) shifted it by 63 more in total.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 2273,
      "reason": "(Line moved 2259 -> 2270 -> 2271 -> 2272: the fixes noted in prior revisions of this entry, then the 2026-09-18 v1 fixes 2 review-fix round's toast-allowlist refresh shifted it by 1 more.) Direct synchronous gate inline in the Publish-checks freshness guard — points to Run Publish Checks, the same user-owned action the message names. Not a review/round outcome. (Line moved 2214 -> ... -> 2259, see reviewActions.ts:8232's note.)"
    },
    {
      "file": "src/commands/runLintingFixes.ts",
      "line": 285,
      "reason": "Direct synchronous gate inline in the user-invoked 'AI final fixes' command when no usable Publish check result is recorded — points to Run Publish Checks, the action the message names. Not a review/round outcome."
    },
    {
      "file": "src/commands/runLintingFixes.ts",
      "line": 330,
      "reason": "Direct synchronous gate inline in the user-invoked 'AI final fixes' command when the persisted Publish verification scope is stale — points to Run Publish Checks to choose a new scope. Not a review/round outcome."
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 3039,
      "reason": "(Line moved 3009 -> 3036 -> 3037 -> 3038, see reviewActions.ts:2272's note.) Same-model retry offered after every configured backup has also failed to produce a parseable review this episode — a one-click retry of the exact action that just ran, not a fallback-chain choice (remains available regardless of the Never switch setting, Part 5 step 15). (Line moved 2954 -> ... -> 3009, see reviewActions.ts:2259's note.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 9059,
      "reason": "(Line moved 8874 -> 8965 -> 8985 -> 8995 -> 9032 -> 9058, see reviewActions.ts:9695's note.) Publish Anyway nudge pointing at commitAndPushTask when the Complete & Move On Publish review could not be started automatically (another review in progress) — the drop reason is already logged; this only offers the manual alternative. (Line moved 8484 -> ... -> 8874, see reviewActions.ts:8329's note; a later 2026-09-18 review-fix round's item-1 fix (built in two edits) shifted it by 63 more in total.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 9085,
      "reason": "(Line moved 8900 -> 8991 -> 9011 -> 9021 -> 9058 -> 9084, see reviewActions.ts:9695's note.) Publish Anyway nudge pointing at commitAndPushTask when the entry-owned publish preflight check failed — the failure reason is named in the message; this only offers the manual alternative. (Line moved 8510 -> ... -> 8900, see reviewActions.ts:8329's note; a later 2026-09-18 review-fix round's item-1 fix (built in two edits) shifted it by 63 more in total.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 9100,
      "reason": "(Line moved 8915 -> 9006 -> 9026 -> 9036 -> 9073 -> 9099, see reviewActions.ts:9695's note.) Publish nudge pointing at commitAndPushTask when the entry-owned publish preflight check passed — Commit and Push must only ever run from the user's own click, so this only nudges toward it. (Line moved 8525 -> ... -> 8915, see reviewActions.ts:8329's note; a later 2026-09-18 review-fix round's item-1 fix (built in two edits) shifted it by 63 more in total.)"
    },
    {
      "file": "src/commands/setTaskStage.ts",
      "line": 313,
      "reason": "Publish Anyway nudge pointing at commitAndPushTask when an explicit stage-set to Publish finds failing completion checks — the same user-owned, never-auto-scheduled action every sibling Publish Anyway nudge in reviewActions.ts points at."
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 10796,
      "reason": "(Line moved 10611 -> 10702 -> 10722 -> 10732 -> 10769 -> 10795, see reviewActions.ts:9695's note.) Nudge to run this stage's review after an Implementation round reported nothing to fix — the round's own outcome is recorded in the run log and chat ledger; this toast only points at the single follow-up command (Run Review) already named in the message text. (Line moved 10096 -> ... -> 10611, see reviewActions.ts:8329's note; a later 2026-09-18 review-fix round's item-1 fix (built in two edits) shifted it by 63 more in total.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 11001,
      "reason": "(Line moved 10816 -> 10907 -> 10927 -> 10937 -> 10974 -> 11000, see reviewActions.ts:10732's note.) Best-effort fallback for when postReconcilePlanChecklistDecisionV1 could not post the chat decision (no activating extension context) — the checklist-under-recording case (the checklist still shows unfinished items even though the most recent review already scored full marks with zero blockers). (Line moved 10290 -> ... -> 10816, see reviewActions.ts:10611's note; a later 2026-09-18 review-fix round's item-1 fix (built in two edits) shifted it by 63 more in total.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 11258,
      "reason": "(Line moved 11073 -> 11164 -> 11184 -> 11194 -> 11231 -> 11257, see reviewActions.ts:10732's note.) Best-effort fallback for when postReconcilePlanChecklistDecisionV1 could not post the chat decision — same fallback-of-a-migrated-decision pattern as copilotImplementationRunner.ts:794, just for the checklist-reconciliation decision instead of the round-limit gate. (Line moved 10547 -> ... -> 11073, see reviewActions.ts:10611's note; a later 2026-09-18 review-fix round's item-1 fix (built in two edits) shifted it by 63 more in total.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 11751,
      "reason": "(Line moved 11566 -> 11657 -> 11677 -> 11687 -> 11724 -> 11750, see reviewActions.ts:10732's note.) Best-effort fallback for when postReconcilePlanChecklistDecisionV1 could not post the chat decision — the unverified-step-counts case. (Line moved 11185 -> ... -> 11566, see reviewActions.ts:10611's note; a later 2026-09-18 review-fix round's item-1 fix (built in two edits) shifted it by 63 more in total.)"
    },
    {
      "file": "src/commands/startNewTask.ts",
      "line": 525,
      "reason": "(Line moved 516 -> 525: the 2026-09-17 v1 fixes 2 Wave I review-fix round's nextActor chokepoint write at task creation added a setNextActorV1 import and a multi-line writeTaskProgressV1 call above it.) Direct synchronous notice inline in the user-invoked Start New Task command that the new task landed paused because another task is already active — 'Resume' is the one-click follow-up to the command that just ran, not a background round decision."
    },
    {
      "file": "src/commands/taskCreationRecovery.ts",
      "line": 293,
      "reason": "Direct synchronous notice inline in the user-invoked task-creation Retry command that the resumed task landed paused (see startNewTask.ts:516 for the same pattern)."
    },
    {
      "file": "src/commands/taskCreationRecovery.ts",
      "line": 554,
      "reason": "Direct synchronous notice inline in the user-invoked task-creation Adopt-and-Retry command that the resumed task landed paused (see startNewTask.ts:516 for the same pattern)."
    },
    {
      "file": "src/views/chatView.ts",
      "line": 2462,
      "reason": "Chat-history recovery notice (plan §5.1) — a corrupted local chat-v1.json needs 'Reset Chat History' or manual inspection via 'Ensemble: Open Chat Data'; a data-recovery affordance, not a task review/round workflow decision."
    },
    {
      "file": "src/commands/scheduleTaskResume.ts",
      "line": 547,
      "reason": "v1 fixes item 1, Part 1c step 16 — the periodic sweep's takeover notice for a work-admission marker whose owner has stayed persistently unreachable/unresponsive/unreadable. This is workflow-control bookkeeping about Ensemble's own admission-directory lock file (already classified `workflowControl` by classifyWorkflowPathV1, same as `.ensemble-session.lock`), not a task review/round content decision — nothing here asks what the AI should do next, only who owns a stale lock. 'Non-pausing notice' by the plan's own wording, which also rules out the pausing escalation-card system as the right surface. (Line moved 499 -> 500 -> 504: the first shift on 2026-09-15 by an added `RUNS_DIRNAME` import earlier in the file, the second the same day by the post-freeze findings item 5 `retireSatisfiedSummaryRejectedRecoveryV1` import addition to the same import block.)"
    }
  ]
}
