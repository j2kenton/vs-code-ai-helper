{
  "$schema": "n/a — read by scripts/verifyToastAllowlistV1.mjs",
  "mode": "enforcing",
  "note": "Every vscode.window.showInformationMessage/showWarningMessage call under src/ that offers MORE THAN ONE button must either be converted to a WorkflowDecisionV1/StructuredQuestionV1 posted in the chat, or be listed here with a reason it genuinely is NOT a workflow decision (task 'stage chat as a record of work', item 13 / Part 12 step 35e). A single-button call never needs an entry — the lint only flags calls that ask the user to choose between options. Entries are keyed by file:line, so a line-number drift on an untouched call is expected to surface as a stale entry on the next run — update the line, do not delete the entry, unless the call itself was removed or reduced to one button.",
  "entries": [
    {
      "file": "src/commands/commitAndPushTask.ts",
      "line": 336,
      "reason": "Direct synchronous confirmation inside the user-invoked Commit & Push flow — the modal preview of the exact commit message and destination before it ships, with an inline 'Regenerate' retry. Not a review/round workflow decision. (Line moved 332 -> 333 -> 334 -> 335 -> 336 as the 2026-09-11 target-resolution write-failure fix, the shared early-admission-helper wiring fix, the 2026-09-14 unprotectedRootPaths check, and the same day's pause-resolver import each added an import/block above it; call is unchanged.)"
    },
    {
      "file": "src/commands/commitAndPushTask.ts",
      "line": 1797,
      "reason": "Direct synchronous confirmation inside the user-invoked Publish flow when completion checks fail — 'Publish Anyway' vs 'Fix with AI' is answered in the same synchronous command invocation, not posted from a background round. (Line moved 1793 -> 1794 -> 1795 -> 1796 -> 1797, see commitAndPushTask.ts:336's note.)"
    },
    {
      "file": "src/commands/commitAndPushTask.ts",
      "line": 2191,
      "reason": "Direct synchronous confirmation inside the user-invoked Commit & Push flow, offering to view the full file list before an outward-facing, largely irreversible push. (Line moved 2187 -> 2188 -> 2189 -> 2190 -> 2191, see commitAndPushTask.ts:336's note.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 8793,
      "reason": "Direct synchronous response to the user explicitly invoking 'View Review' when no review artifact exists yet — offers to dispatch a review or create the file by hand. Not a decision surfaced from an automated round. (Line moved 8010 -> ... -> 8738 -> 8739 as the fixes noted in prior revisions of this entry, landing at 8739, then a prior round's Part 4 Step 12 fix (offering 'Run Implementation' on the unusable-summary refusal path, earlier in this file) added 34 more lines above it, net, landing at 8773, then a prior round's item-16 no-op-continuation correctness fix (the `providerInvokedThisRound` tracking added to `runImplementationWithAI`, earlier in this file) added 2 more lines above it, net, landing at 8775, then this round's fix decoupling the unusable-summary refusal messages' 'offer Run Implementation' condition from restorability (2026-09-24 review, narrowed completion blocker; three call sites earlier in this file, each restructured to check `offerRunImplementation`/`shouldOfferRunImplementationForUnusableSummaryV1` first) added 18 more lines above it, net, landing at 8793; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 10161,
      "reason": "Pre-run safety check synchronously inline in the user-invoked 'Run Implementation' command (unrelated uncommitted changes in the workspace) — a git-safety confirmation, not a review/round outcome. (Line moved 9181 -> ... -> 10106 -> 10107 as the fixes noted in prior revisions of this entry, landing at 10107, then a prior round's Part 4 Step 12 fix (offering 'Run Implementation' on the unusable-summary refusal path, earlier in this file) added 34 more lines above it, net, landing at 10141, then a prior round's item-16 no-op-continuation correctness fix (the `providerInvokedThisRound` tracking added to `runImplementationWithAI`, earlier in this file) added 2 more lines above it, net, landing at 10143, then this round's fix decoupling the unusable-summary refusal messages' 'offer Run Implementation' condition from restorability (see reviewActions.ts:8793's note) added 18 more lines above it, net, landing at 10161; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/runLintingFixes.ts",
      "line": 578,
      "reason": "Pre-run safety check synchronously inline in the user-invoked 'AI final fixes' command, mirroring reviewActions.ts:7690 for the same git-safety concern in a different entry point."
    },
    {
      "file": "src/extension.ts",
      "line": 2047,
      "reason": "Activation-time recovery for an interrupted revert-journal swap found on disk — the plan's own named example of an expected allow-list entry ('revert-journal restore'). Resolves a durable mid-flight artifact, not a task review/round decision."
    },
    {
      "file": "src/utils/aiConsent.ts",
      "line": 57,
      "reason": "The one-time legal/usage disclaimer gate shown before the very first AI command of a session — modal by design, and 'View Disclaimer' loops back rather than branching workflow state. Not a task decision."
    },
    {
      "file": "src/utils/metaResourcesMigration.ts",
      "line": 365,
      "reason": "One-time workspace housekeeping offer (move a legacy resource folder to .ensemble) shown at most once per workspace, unrelated to any task's review/round workflow."
    },
    {
      "file": "src/utils/promptSizeGuard.ts",
      "line": 92,
      "reason": "Direct synchronous pre-dispatch size warning inline in whichever command is about to send an oversized prompt — a cost/quota guard on the calling command's own flow, not a review/round outcome to record in chat."
    },
    {
      "file": "src/runners/copilotImplementationRunner.ts",
      "line": 572,
      "reason": "Fallback of the already-migrated round-limit gate (see the awaitWorkflowDecisionAnswerV1 call a few lines above) for the one case with no task to attribute a chat decision to (no taskFolderUri/stage in scope). The task-scoped path never reaches this line. (Line moved 794 -> 806 as the 2026-09-15 post-freeze findings item 5 workflow-control exclusion added to executeToolCall's write_file/delete_file tool handlers, above it, added 12 lines, then a Part 3 `resumeKind` classification pass (the `continue`/`cancel` option literals for this same gate) added 2 more lines above it, then a later round extracted the whole gate — including this fallback — out of its former place inline inside `runImplementationWithCopilot`'s tool-call loop into the standalone, exported `resolveRoundLimitDecisionV1` function defined near the top of the file, so the call now sits at line 572 instead of 808; the extraction changed the line only, not the fallback's behaviour or its no-taskFolderUri/stage condition.)"
    },
    {
      "file": "src/config/settings.ts",
      "line": 328,
      "reason": "Activation-time notice that workspace and user settings values differ, offering to reconcile them — a settings-hygiene notice unrelated to any task's review/round workflow, and not gating task progress."
    },
    {
      "file": "src/views/settingsView.ts",
      "line": 566,
      "reason": "Settings-view notice about leftover per-task model overrides from a superseded configuration shape, offering to clear them — settings hygiene, not a task workflow decision."
    }
  ],
  "directActionEntriesNote": "Every NotificationRouter.show(Information/Warning/Error) call under src/ whose actionCommand dispatches a workflow command directly (rather than the chat-pointer vs-code-ai-helper.openWorkflowDecision) must either be converted to point at a WorkflowDecisionV1 in the chat, or be listed here with a reason it is NOT a hidden workflow decision (task 'stage chat as a record of work', item 13 / Part 12 step 35e, review round 2). Two shapes qualify: (1) a synchronous gate/nudge inline in the user-invoked command it belongs to, pointing at a command already reachable another way (Command Palette, another button) and never the only record of a background round's outcome; (2) a best-effort fallback for when a WorkflowDecisionV1 could not be posted to chat (no activating extension context), degrading to a single pointer at the same option the decision would have recommended.",
  "directActionEntries": [
    {
      "file": "src/commands/resumeTask.ts",
      "line": 754,
      "reason": "(Line moved 753 -> 754: this round's fix updating the stale dispatchesAutomationWork doc comment above added 1 line, net.) v1 fixes 2, items 14 + 25: direct synchronous refusal in showResumeStaysPausedV1, shared by resumeAndApplyCurrentStageActionV1, resumeAndRerunReviewV1 and resumeAndDispatchImplementationV1 — shown the instant a user-invoked Resume is declined because the Review it would arrange would be refused (the last round left the implementation summary unusable). Offers restoreRejectedImplementationRoundV1, the same deterministic artifact restore the review-dispatch refusal offers; no rerun command is passed because the task stays paused. Not a review/round outcome posted from a background round."
    },
    {
      "file": "src/utils/taskOperations.ts",
      "line": 1646,
      "reason": "(Line moved 1535 -> 1573 -> 1579 -> 1628 -> 1579 -> 1611 -> 1646: this round's fix teaching cancelRunningOperationsForTask to detect a forced end during its own call — subscribing to onDidEnd and reporting forcedEnd — added 38 lines above it, net, then this round's completion-blocker fix (naming the CancelRunningOperationsResultV1 result type, exported for setTaskStage/resumeTask.ts to share) added 6 more lines above it, net, then a round's fix for the narrowed completion blocker 906d1f21-e807-48d3-9468-62856cdc7e7a-0 (adding requestLocalCancellationForTaskV1, the request-only cancellation helper setTaskStage called from inside enterStageV1's locked beforeWrite, plus its doc comment) added 49 more lines above it, net, then this round reverted requestLocalCancellationForTaskV1 entirely as architecturally unsafe (setTaskStage now requests cancellation only post-commit, via the existing cancelRunningOperationsForTask call) — removing the helper, its doc comment, and a leftover blank line, net -49 lines, then a later round added the settleAs outcome-override primitive (TaskOperationHandle.settleAs, getExplicitOutcome, and runTrackedOperation's precedence check) above it in this same file, adding 32 lines net, then a further completion-blocker line-number refresh found 35 more lines added above it, landing at 1646.) v1 fixes 2, item 21 (2026-09-18 round): direct synchronous refusal inline in showTaskBusyWarning — the busy-guard refusal shown the instant a user-invoked command is declined because a task's exclusive lock is held, offered only when isUnrecoverableImplRecoveryV1 confirms the named implRecovery record has no automated way back. Same pattern and same discardOwedImplRecoveryV1 command as reviewActions.ts's Complete Stage & Move On catch-block entry above — this is the OTHER production surface item 21 requires the discard action reachable from, not a review/round outcome posted from a background round."
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 4288,
      "reason": "Synchronous pointer on the 'Code reviews passed' notice at the gate before Publish (auto-advance off): the user's own next click, 'Complete Stage & Move On', already reachable from the task tree and Command Palette. It is never the only record of the round's outcome, which the chat already carries (v1 fixes 2, item 33). (Line moved 4295 -> ... -> 4284 -> 4285 as the fixes noted in prior revisions of this entry, landing at 4285, then a prior round's import-block addition (a new `shouldOfferRunImplementationForUnusableSummaryV1` import for the Part 4 Step 12 fix) added 1 more line above it, net, landing at 4286, then a prior round's `shouldLogNoOpContinuationRoundV1` import addition (also above it) added 1 more line, net, landing at 4287, then this round's own new import additions earlier in the file (2026-09-24 review, narrowed completion blocker fix decoupling the unusable-summary refusal toasts' 'offer Run Implementation' condition from restorability) added 1 more line above it, net, landing at 4288; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 13784,
      "reason": "v1 fixes 2, item 21 (2026-09-18 review-fix round): direct synchronous refusal inline in Apply Review's own runApply guard, shown the instant a user-invoked Apply Review is declined because the review was invalidated by a still-owed recovery continuation. Same pattern and same discardOwedImplRecoveryV1 command as the Complete Stage & Move On catch-block entry and taskOperations.ts's busy-guard entry above — this is the THIRD production surface item 21 requires the discard action reachable from (a completion blocker: the review found this refusal named the owed continuation without offering the one action that clears it). (Line moved 13445 -> ... -> 13722 -> 13762 as the fixes noted in prior revisions of this entry, landing at 13762, then this round's fix refreshing the stage-entry recovery lock source scan's exact-line allowlist (2026-09-24 review, narrowed completion blocker `04617511-d1ba-4902-adb7-87371ca86dbf-1`) found this entry itself stale by +22 lines against the already-present working-tree edits it was tracking — no lines were added by this round's own change; landing at 13784; call is unchanged, verified by content match on the `discardOwedImplRecoveryV1`/'Discard This Owed Continuation' text.)"
    },
    {
      "file": "src/commands/chatWithStage.ts",
      "line": 742,
      "reason": "Synchronous pre-dispatch gate inline in the user-invoked Chat Send command when no model is configured for the stage — points to Ensemble Settings to fix the configuration, not a review/round decision. (Line moved 589 → 601 → 612 → 605 → 633 → 653 → 659 → 667 → 677 → 689 → 695 as admission-registration edits, including the 2026-09-14 ownership-legitimacy-signal doc-comment update, landed on this route; call is unchanged.)"
    },
    {
      "file": "src/commands/draftTaskWithAI.ts",
      "line": 420,
      "reason": "Synchronous pre-dispatch gate inline in the user-invoked Draft with AI command when no model is configured for the Description stage — points to Ensemble Settings, not a review/round decision. (Line moved 384 → 385 → 386 → 385 → 417 → 418 → 419 as admission-registration edits, including the 2026-09-14 unprotectedRootPaths check, landed on this route; call is unchanged.)"
    },
    {
      "file": "src/commands/generatePlanWithAI.ts",
      "line": 804,
      "reason": "Synchronous pre-dispatch gate inline in the user-invoked Generate Plan command when no model is configured for the Plan stage — points to Ensemble Settings, not a review/round decision. (Line moved 734 -> 775 -> 791 -> 804: the 2026-09-22 pre-1.0.0 fixes register round's second review-fix follow-up added the enterStageV1-routed refusal branch above this call (net +41), then a completion-blocker fix (reporting that refusal as succeeded: false, plus a stageEntryRefusalReason variable and run-log addendum) added 16 more lines above it, then this round's review-pass-18 fix (routing the tracked operation's terminal state through refusedWhenFalse, with an explanatory comment) added 13 more lines above it.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 12952,
      "reason": "Synchronous pre-dispatch gate inline in the create-implementation-checklist path when no model is configured for Implementation — points to Ensemble Settings, not a review/round decision. (Line moved 11736 -> ... -> 12890 -> 12930 as the fixes noted in prior revisions of this entry, see reviewActions.ts:9695's note, landing at 12930, then this round's fix refreshing the stage-entry recovery lock source scan's exact-line allowlist (2026-09-24 review, narrowed completion blocker `04617511-d1ba-4902-adb7-87371ca86dbf-1`) found this entry itself stale by +22 lines against the already-present working-tree edits it was tracking — no lines were added by this round's own change; landing at 12952; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/markTaskDone.ts",
      "line": 183,
      "reason": "Direct synchronous response to the user-invoked Complete Task command when the Publish artifact is missing — 'Complete Anyway' re-invokes the same command with an explicit override, not a decision surfaced from a background round."
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 5348,
      "reason": "(Line moved 5091 -> ... -> 5330 -> 5331 -> 5336: the fixes noted in prior revisions of this entry, landing at 5331, then a prior round's Part 4 Step 12 fix (item 16 — offering 'Run Implementation' as a second, nested-ternary branch of the same actionCommand argument when nothing is restorable and no implRecovery is armed) added 5 more lines above it, net, landing at 5336, then a prior round's fix removing the unapproved `checklistFullySettled` restriction from `shouldOfferRunImplementationForUnusableSummaryV1`'s signature (a same-line-count edit at this exact call) plus the `shouldLogNoOpContinuationRoundV1` import addition above it added 1 more line, net, landing at 5337, then this round's fix decoupling this exact toast's 'offer Run Implementation' condition from restorability (2026-09-24 review, narrowed completion blocker: the toast used to show 'Run Implementation' only when nothing was restorable — narrower than the checked item's own applicability condition) restructured the ternary and added a `hasLiveImplRecovery` read plus an 11-line doc comment above the call, net +11 lines, landing at 5348; the actionCommand argument now leads with `shouldOfferRunImplementationForUnusableSummaryV1(hasLiveImplRecovery)`, so Run Implementation is the FIRST `command:` literal the scanner sees at this line — see the entry immediately below and the two Fast Forward entries further down, which needed independent entries for the exact same reason once this round's fix made the restore branch no longer first.) Direct synchronous refusal inline in runReviewForFolder's own unusable-implementation-summary gate — offers to restore impl-summary_prev.md when it is itself usable (2026-09-15 post-freeze findings, item 3: a refusal must name a remedy the system can actually reach), or Run Implementation directly when no live implRecovery is armed, whichever this round's fix determines takes priority. restoreRejectedImplementationRoundV1 and resumeAndDispatchImplementationV1 are both deterministic dispatches already reachable elsewhere (the tree row, the Command Palette); this only offers the same action at a second refusal surface. Not a review/round outcome posted from a background round. (Line moved 4973 -> 4988 -> 4989 -> 4994 -> 5023 -> 5032 -> 5081 -> 5091 when the completion-review fix threaded a rerunCommandId option through runReviewForFolder so this button can also re-dispatch the refused stage after a successful restore, then the same day's withDeterministicZeroFilesChangedNoteV1 import addition shifted it by one more line, then the same day's post-freeze findings item 5 import additions noted at reviewActions.ts:8329 shifted it by 5 more, then the same day's post-freeze findings item 4 quotaCandidate rework noted at reviewActions.ts:8329 shifted it by 29 more, then that same finding's typed deferredFailureKind follow-up noted at reviewActions.ts:8329 shifted it by 9 more, then that same finding's run-log quota-block note shifted it by 49 more, and then the 2026-09-16 candidatesDeferred outcome-code addition noted at reviewActions.ts:8329 shifted it by 10 more.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 7678,
      "reason": "v1 fixes 2, item 1 (Wave II escape hatches): Fast Forward's OWN unusable-implementation-summary refusal — the third of the three surfaces that must offer 'Restore Last Usable Summary' (the other two are reviewActions.ts:5348's runReviewForFolder branch, and buildReviewResumeVariablesV1's identical branch, which returns its actionCommand to a caller rather than calling NotificationRouter itself). Same restoreRejectedImplementationRoundV1 deterministic artifact restore, reached when Fast Forward tries to run its own initial review before fast-forwarding and that dispatch is refused for the same rejected-round reason. Not a review/round outcome posted from a background round — offered from the same synchronous command invocation the user's Fast Forward click started. (Line moved 7628 -> ... -> 7635 -> 7636 as the fixes noted in prior revisions of this entry, landing at 7636, then a prior round's item-16 fix added 22 more lines above it, net, landing at 7658, then a prior round's `describeUnusableReviewBlockV1` doc-comment fix (correcting the `offerRunImplementation` field's condition) plus the earlier `shouldLogNoOpContinuationRoundV1` import addition together added 2 more lines above it, net, landing at 7660, then this round's fix decoupling this toast's 'offer Run Implementation' condition from restorability (2026-09-24 review, narrowed completion blocker) restructured the ternary so `block.offerRunImplementation` leads and added a 5-line comment ahead of it, plus rephrased the pre-existing 'runReviewForFolder' comment inside the restore branch to remove a stray apostrophe (net +18 lines, landing at 7678) — that apostrophe left this scanner's naive string-tokenizer in an unterminated-quote state for the rest of the argument list, so the FIRST attempt at this round's own new comment (an earlier draft, since corrected) made this call invisible to `findDirectActionCommandCalls` (args.length parsed as < 5); the fix restores correct 5-argument parsing; verified by content match this round.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 7708,
      "reason": "Same as reviewActions.ts:7678 immediately above — Fast Forward's second unusable-summary check point a few lines later (the re-read after runReviewForFolder's own initial-review dispatch attempt comes back with no parseable Readiness score), offering the identical restore action for the identical cause, plus the identical Run Implementation branch (see the entry immediately above's note). (Line moved 7652 -> ... -> 7659 -> 7660, see the entry immediately above's note; then a prior round's item-16 fix added 28 more lines above it, net, landing at 7688, then a prior round's same 2-line shift as the entry immediately above landing at 7690, then this round's same 18-line shift as the entry immediately above landing at 7708; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 9132,
      "reason": "(Line moved 8617 -> ... -> 9077 -> 9078, see reviewActions.ts:9695's note, landing at 9078.) Direct synchronous 'Complete Anyway' gate inline in the stage-advance command when a required completion artifact is missing — re-invokes the same command with an explicit override, the same pattern as markTaskDone.ts:183. (Line moved 8251 -> ... -> 8617, see reviewActions.ts:8329's note; the fixes noted in prior revisions of this entry, landing at 9078, then a prior round's Part 4 Step 12 fix (offering 'Run Implementation' on the unusable-summary refusal path, earlier in this file) added 34 more lines above it, net, landing at 9112, then a prior round's `describeUnusableReviewBlockV1` doc-comment fix plus the `shouldLogNoOpContinuationRoundV1` import addition (both earlier in this file) added 2 more lines above it, net, landing at 9114, then this round's fix decoupling the unusable-summary refusal toasts' 'offer Run Implementation' condition from restorability (2026-09-24 review, narrowed completion blocker; three call sites earlier in this file) added 18 more lines above it, net, landing at 9132; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 9166,
      "reason": "(Line moved 8651 -> ... -> 9111 -> 9112, see reviewActions.ts:9695's note, landing at 9112.) Direct synchronous 'Complete Anyway' gate inline in the stage-advance command when a plan-review blocker is not recorded as resolved — re-invokes the same command with an explicit override, the same pattern as markTaskDone.ts:183. (Line moved 8285 -> ... -> 8651, see reviewActions.ts:8329's note; the fixes noted in prior revisions of this entry, landing at 9112, then a prior round's Part 4 Step 12 fix (offering 'Run Implementation' on the unusable-summary refusal path, earlier in this file) added 34 more lines above it, net, landing at 9146, then a prior round's same 2-line shift as reviewActions.ts:9132's entry landing at 9148, then this round's same 18-line shift as reviewActions.ts:9132's entry landing at 9166; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 9252,
      "reason": "(Line moved 8734 -> ... -> 9197 -> 9198, see reviewActions.ts:9695's note, landing at 9198.) Direct synchronous refusal inline in advanceStageViaNextStageRowV1's own catch block (the 'Complete Stage & Move On' command) when the transition was refused for an owed implementation recovery continuation that has no automated way back (2026-09-15 post-freeze findings, item 5 / Part 5 step 34: 'a dispatched recovery needs a user-reachable escape'). discardOwedImplRecoveryV1 is a confirmed, deterministic clear of the stuck record, offered only when isImplRecoveryDiscardOfferableV1 confirms the record cannot resolve itself. Not a review/round outcome posted from a background round — this is the same synchronous command invocation the user just clicked. (Line moved 8637 -> ... -> 8734, see reviewActions.ts:8329's note; the fixes noted in prior revisions of this entry, landing at 9198, then a prior round's Part 4 Step 12 fix (offering 'Run Implementation' on the unusable-summary refusal path, earlier in this file) added 34 more lines above it, net, landing at 9232, then a prior round's same 2-line shift as reviewActions.ts:9132's entry landing at 9234, then this round's same 18-line shift as reviewActions.ts:9132's entry landing at 9252; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 2275,
      "reason": "(Line moved 2259 -> ... -> 2271 -> 2272: the fixes noted in prior revisions of this entry, landing at 2272, then a prior round's import-block addition (a new `shouldOfferRunImplementationForUnusableSummaryV1` import for the Part 4 Step 12 fix) added 1 more line above it, net, landing at 2273, then a prior round's `shouldLogNoOpContinuationRoundV1` import addition (also above it) added 1 more line, net, landing at 2274, then this round's own new import additions earlier in the file (2026-09-24 review, narrowed completion blocker fix decoupling the unusable-summary refusal toasts' 'offer Run Implementation' condition from restorability) added 1 more line above it, net, landing at 2275; call is unchanged, verified by content match.) Direct synchronous gate inline in the Publish-checks freshness guard — points to Run Publish Checks, the same user-owned action the message names. Not a review/round outcome. (Line moved 2214 -> ... -> 2259, see reviewActions.ts:8232's note.)"
    },
    {
      "file": "src/commands/runLintingFixes.ts",
      "line": 285,
      "reason": "Direct synchronous gate inline in the user-invoked 'AI final fixes' command when no usable Publish check result is recorded — points to Run Publish Checks, the action the message names. Not a review/round outcome."
    },
    {
      "file": "src/commands/runLintingFixes.ts",
      "line": 330,
      "reason": "Direct synchronous gate inline in the user-invoked 'AI final fixes' command when the persisted Publish verification scope is stale — points to Run Publish Checks to choose a new scope. Not a review/round outcome."
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 3084,
      "reason": "(Line moved 3009 -> ... -> 3080 -> 3081, see reviewActions.ts:2275's note, landing at 3081.) Same-model retry offered after every configured backup has also failed to produce a parseable review this episode — a one-click retry of the exact action that just ran, not a fallback-chain choice (remains available regardless of the Never switch setting, Part 5 step 15). (Line moved 2954 -> ... -> 3009, see reviewActions.ts:2259's note; the fixes noted in prior revisions of this entry, landing at 3081, then a prior round's import-block addition (a new `shouldOfferRunImplementationForUnusableSummaryV1` import for the Part 4 Step 12 fix) added 1 more line above it, net, landing at 3082, then a prior round's `shouldLogNoOpContinuationRoundV1` import addition (also above it) added 1 more line, net, landing at 3083, then this round's own new import additions earlier in the file (see reviewActions.ts:2275's note) added 1 more line above it, net, landing at 3084; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 9398,
      "reason": "(Line moved 8874 -> ... -> 9343 -> 9344, see reviewActions.ts:9695's note, landing at 9344.) Publish Anyway nudge pointing at commitAndPushTask when the Complete & Move On Publish review could not be started automatically (another review in progress) — the drop reason is already logged; this only offers the manual alternative. (Line moved 8484 -> ... -> 8874, see reviewActions.ts:8329's note; the fixes noted in prior revisions of this entry, landing at 9344, then a prior round's Part 4 Step 12 fix (offering 'Run Implementation' on the unusable-summary refusal path, earlier in this file) added 34 more lines above it, net, landing at 9378, then a prior round's same 2-line shift as reviewActions.ts:9132's entry landing at 9380, then this round's same 18-line shift as reviewActions.ts:9132's entry landing at 9398; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 9424,
      "reason": "(Line moved 8900 -> ... -> 9369 -> 9370, see reviewActions.ts:9695's note, landing at 9370.) Publish Anyway nudge pointing at commitAndPushTask when the entry-owned publish preflight check failed — the failure reason is named in the message; this only offers the manual alternative. (Line moved 8510 -> ... -> 8900, see reviewActions.ts:8329's note; the fixes noted in prior revisions of this entry, landing at 9370, then a prior round's Part 4 Step 12 fix (offering 'Run Implementation' on the unusable-summary refusal path, earlier in this file) added 34 more lines above it, net, landing at 9404, then a prior round's same 2-line shift as reviewActions.ts:9132's entry landing at 9406, then this round's same 18-line shift as reviewActions.ts:9132's entry landing at 9424; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 9439,
      "reason": "(Line moved 8915 -> ... -> 9384 -> 9385, see reviewActions.ts:9695's note, landing at 9385.) Publish nudge pointing at commitAndPushTask when the entry-owned publish preflight check passed — Commit and Push must only ever run from the user's own click, so this only nudges toward it. (Line moved 8525 -> ... -> 8915, see reviewActions.ts:8329's note; the fixes noted in prior revisions of this entry, landing at 9385, then a prior round's Part 4 Step 12 fix (offering 'Run Implementation' on the unusable-summary refusal path, earlier in this file) added 34 more lines above it, net, landing at 9419, then a prior round's same 2-line shift as reviewActions.ts:9132's entry landing at 9421, then this round's same 18-line shift as reviewActions.ts:9132's entry landing at 9439; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/setTaskStage.ts",
      "line": 535,
      "reason": "(Line moved 313 -> 326 -> 339 -> 447 -> 443 -> 458 -> 497 -> 533 -> 537 -> 535 as earlier edits to this file shifted it, most recently this round's fix for the reviewer's completion blocker on the second, blind cancelRunningOperationsForTask call in resumeAndSetTaskStageV1 (a cancelResultOutV1 out-box on SetTaskStageArg, its doc comment, normalizeArg plumbing, and the deposit beside the cancelRunningOperationsForTask call here) added 39 lines above it, net, then a round's fix for the narrowed completion blocker 906d1f21-e807-48d3-9468-62856cdc7e7a-0 (an additionalBeforeWrite option requesting cancellation atomically inside enterStageV1's locked commit, plus an expanded comment above the post-commit cancelRunningOperationsForTask call) added 40 more lines above it, net, then this round reverted that option as architecturally unsafe and replaced both comment blocks with an explanation of why cancellation is requested only post-commit, net -2 lines.) Publish Anyway nudge pointing at commitAndPushTask when an explicit stage-set to Publish finds failing completion checks — the same user-owned, never-auto-scheduled action every sibling Publish Anyway nudge in reviewActions.ts points at."
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 11198,
      "reason": "(Line moved 10611 -> ... -> 11143 -> 11144, see reviewActions.ts:9695's note, landing at 11144.) Nudge to run this stage's review after an Implementation round reported nothing to fix — the round's own outcome is recorded in the run log and chat ledger; this toast only points at the single follow-up command (Run Review) already named in the message text. (Line moved 10096 -> ... -> 10611, see reviewActions.ts:8329's note; the fixes noted in prior revisions of this entry, landing at 11144, then a prior round's Part 4 Step 12 fix (offering 'Run Implementation' on the unusable-summary refusal path, earlier in this file) added 34 more lines above it, net, landing at 11178, then a prior round's same 2-line shift as reviewActions.ts:9132's entry landing at 11180, then this round's same 18-line shift as reviewActions.ts:9132's entry landing at 11198; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 11403,
      "reason": "(Line moved 10816 -> ... -> 11348 -> 11349, see reviewActions.ts:10732's note, landing at 11349.) Best-effort fallback for when postReconcilePlanChecklistDecisionV1 could not post the chat decision (no activating extension context) — the checklist-under-recording case (the checklist still shows unfinished items even though the most recent review already scored full marks with zero blockers). (Line moved 10290 -> ... -> 10816, see reviewActions.ts:10611's note; the fixes noted in prior revisions of this entry, landing at 11349, then a prior round's Part 4 Step 12 fix (offering 'Run Implementation' on the unusable-summary refusal path, earlier in this file) added 34 more lines above it, net, landing at 11383, then a prior round's same 2-line shift as reviewActions.ts:9132's entry landing at 11385, then this round's same 18-line shift as reviewActions.ts:9132's entry landing at 11403; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 11662,
      "reason": "(Line moved 11073 -> ... -> 11607 -> 11608, see reviewActions.ts:10732's note, landing at 11608.) Best-effort fallback for when postReconcilePlanChecklistDecisionV1 could not post the chat decision — same fallback-of-a-migrated-decision pattern as copilotImplementationRunner.ts:794, just for the checklist-reconciliation decision instead of the round-limit gate (the checklistClaimedButUnmerged case). (Line moved 10547 -> ... -> 11073, see reviewActions.ts:10611's note; the fixes noted in prior revisions of this entry, landing at 11608, then a prior round's Part 4 Step 12 fix (offering 'Run Implementation' on the unusable-summary refusal path, earlier in this file) added 34 more lines above it, net, landing at 11642, then a prior round's same 2-line shift as reviewActions.ts:9132's entry landing at 11644, then this round's same 18-line shift as reviewActions.ts:9132's entry landing at 11662; call is unchanged, verified by content match against the `checklistClaimedButUnmergedWithoutClearingReview` branch.)"
    },
    {
      "file": "src/commands/reviewActions.ts",
      "line": 12238,
      "reason": "(Line moved 11566 -> ... -> 12199 -> 12217, see reviewActions.ts:10732's note, landing at 12217.) Best-effort fallback for when postReconcilePlanChecklistDecisionV1 could not post the chat decision — the unverified-step-counts case. (Line moved 11185 -> ... -> 11566, see reviewActions.ts:10611's note; the fixes noted in prior revisions of this entry, landing at 12217, then this round's fix refreshing the stage-entry recovery lock source scan's exact-line allowlist (2026-09-24 review, narrowed completion blocker `04617511-d1ba-4902-adb7-87371ca86dbf-1`) found this entry itself stale by +21 lines against the already-present working-tree edits it was tracking — no lines were added by this round's own change; landing at 12238; call is unchanged, verified by content match.)"
    },
    {
      "file": "src/commands/startNewTask.ts",
      "line": 525,
      "reason": "(Line moved 516 -> 525: the 2026-09-17 v1 fixes 2 Wave I review-fix round's nextActor chokepoint write at task creation added a setNextActorV1 import and a multi-line writeTaskProgressV1 call above it.) Direct synchronous notice inline in the user-invoked Start New Task command that the new task landed paused because another task is already active — 'Resume' is the one-click follow-up to the command that just ran, not a background round decision."
    },
    {
      "file": "src/commands/taskCreationRecovery.ts",
      "line": 293,
      "reason": "Direct synchronous notice inline in the user-invoked task-creation Retry command that the resumed task landed paused (see startNewTask.ts:516 for the same pattern)."
    },
    {
      "file": "src/commands/taskCreationRecovery.ts",
      "line": 554,
      "reason": "Direct synchronous notice inline in the user-invoked task-creation Adopt-and-Retry command that the resumed task landed paused (see startNewTask.ts:516 for the same pattern)."
    },
    {
      "file": "src/views/chatView.ts",
      "line": 2463,
      "reason": "(Line moved 2462 -> 2463: the pre-1.0.0 fixes register item 16 waiting-for-you work added a `withWaitingForHumanFallbackV1` import above it.) Chat-history recovery notice (plan §5.1) — a corrupted local chat-v1.json needs 'Reset Chat History' or manual inspection via 'Ensemble: Open Chat Data'; a data-recovery affordance, not a task review/round workflow decision."
    },
    {
      "file": "src/commands/scheduleTaskResume.ts",
      "line": 911,
      "reason": "v1 fixes item 1, Part 1c step 16 — the periodic sweep's takeover notice for a work-admission marker whose owner has stayed persistently unreachable/unresponsive/unreadable. This is workflow-control bookkeeping about Ensemble's own admission-directory lock file (already classified `workflowControl` by classifyWorkflowPathV1, same as `.ensemble-session.lock`), not a task review/round content decision — nothing here asks what the AI should do next, only who owns a stale lock. 'Non-pausing notice' by the plan's own wording, which also rules out the pausing escalation-card system as the right surface. (Line moved 499 -> 500 -> 504 -> 548 -> 619 -> 880 -> 891 -> 899 -> 911: the fixes noted in prior revisions of this entry, then a Part 3 `resumeKind` classification pass added 8 more lines above it, net, then a `restoreSummary` dispatch fix (making it actually retry after a restore) added 12 more lines above it, net.)"
    }
  ]
}
